# Synthetic Data

The public portfolio version uses only synthetic data generated by `src/synthetic_data.py`.

`data/synthetic/logistics_demo.csv` is intentionally fictitious. It preserves the structural shape of a logistics dashboard dataset while avoiding real operational records, credentials, people, providers, exhibitors, deployment URLs, and private source identifiers.
